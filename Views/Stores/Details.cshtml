@*@model InventoryApp.Models.Store*@
@model InventoryApp.ViewModels.EmployeesDetailsViewModel

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Store</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Store.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Store.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Store.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Store.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Store.IsActive)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Store.IsActive)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Store.OpenDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Store.OpenDate)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Store.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>


<p>
    @Html.ActionLink("Create New", "../Inventories/Create", new { storeId = Model.Store.Id })
</p>

<div id="PartialRender">
    @*@Html.Partial("_InventoryList", Model.Inventories)*@
</div>

@section Scripts {
<script type="text/javascript">

    $(document).ready(function () {
        $("#PartialRender").load(
            '@(Url.Action("GetPartialView", "Stores",null, Request.Url.Scheme))?Id='+ @Model.Store.Id
         );
    });

</script>
}